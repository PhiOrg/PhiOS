#include <phiio.h>
#include <vga.h>

void printk(str) char str[];
{
    /* Now you can write the function.
    p_uint32 *p = &str;
    vga_putString(*p);
    p++;
    vga_putUnsignedNumber(*p);
    vga_putChar('\n');
    p++;
    vga_putString(*p);
    p++;
    vga_putUnsignedNumber(*p);
    */
}

